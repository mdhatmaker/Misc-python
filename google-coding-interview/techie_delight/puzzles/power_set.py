import sys


# https://www.techiedelight.com/generate-power-set-given-set/

# TODO: do this problem!!!

# Generate power set for a given set.
# A power set of any set S is the set of all subsets of S, including the empty set and S itself.



def get_power_set1(S):
    if len(S) == 0: return S
    return

def get_power_set2(S):
    s = S.values();
    for i in range(len(s)):
        rv = {}
        for j in range(len(s)):
            rv[]
    if len(S) == 0: return S
    return

if __name__ == "__main__":

    S = {1:1, 2:1, 3:1}
    get_power_set2(S)
